const alunos = ['Flávio', 'Maria', 'Alfredo', 'Júlia'];

alunos.forEach((item, index) => {
    console.log(`Posição (${index}): ${item}`);
});

console.log('\n');

alunos.forEach((item) => console.log(`Valor: ${item}`));

